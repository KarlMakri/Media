{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}

    <div class="container">
        <h1>Create new User</h1>

        {{ include('user/_form.html.twig') }}<br><br>

        <a href="{{ path('user_index') }}" class="btn btn-info">Liste</a>
    </div>

{% endblock %}
